// 等高線計算用の定数
export const CONTOUR_RESOLUTION = 80;

// キャンバス描画用の定数  
export const CANVAS_SIZE = 500;

// アニメーション速度の定数
export const DEFAULT_ANIMATION_SPEED = 500;

// GA設定のデフォルト値
export const DEFAULT_POPULATION_SIZE = 100;
export const DEFAULT_GENERATIONS = 50;
export const DEFAULT_FUNCTION_TYPE = "sphere" as const;

// UI制約値の定数
export const MIN_POPULATION_SIZE = 1;
export const MAX_POPULATION_SIZE = 1000;
export const MIN_GENERATIONS = 1;
export const MAX_GENERATIONS = 1000;

// 軸ラベル表示の閾値
export const AXIS_LABEL_THRESHOLD = 0.1;